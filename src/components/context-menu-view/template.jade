ul(role='items')
  each item, index in items
    if item.separated
      li.separator
    li.item(index="#{index}" title=item.title ? "#{item.title}" : "" disabled=item.disabled ? "disabled" : undefined)
      - var anchor = item.anchor || {}
      a(href=anchor.url download=anchor.download target=anchor.target)= item.name
