if permissions.user.reinvite
  button.ss.bg-action.hover.actioneer(role="resend-invite") Resend Invite Email
if permissions.user.impersonate
  |  
  a(href="#{model.impersonateUrl()}")
    button.ss
      i.fa.fa-eye
      |  Impersonate
  |  
  button.ss.bg-action.hover.actioneer(role="youtube-sync") 
    i.fa.fa-refresh
    |  Sync YouTube

  |  
  if !model.hasGoldService()
    button.ss.bg-action.hover.actioneer(role="give-gold") 
      i.fa.fa-credit-card
      |  Give Gold Subscription
  if model.hasFreeGold()
    button.ss.bg-action.hover.actioneer(role="revoke-gold") 
      i.fa.fa-minus-circle
      |  Revoke Free Gold

br
br
table.ss.rows
  tbody
    tr
      td
        label.ss.heavy User Type
      td.types

    tr.alternative-names
      td
        label.ss.heavy Alternative Names
      td.names

    tr
      td
        label.ss.heavy Claims Ids
      td.claims
        .ids